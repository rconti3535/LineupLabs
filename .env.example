# Copy to .env and fill in. For Railway, set in dashboard Variables.
# If password has # or @, use %23 and %40 in the URL.
#
# Supabase – if you get ENETUNREACH (e.g. on Railway), use the CONNECTION POOLER:
#   Dashboard → Project Settings → Database → "Connection string" → "URI" under "Connection pooling"
#   Use the Session or Transaction mode URL (port 6543, host like aws-0-REGION.pooler.supabase.com).
#   Same password; add ?sslmode=require if not present. This avoids port 5432 being blocked.
#
# Direct (port 5432):
# DATABASE_URL=postgresql://postgres:PASSWORD@db.xxx.supabase.co:5432/postgres?sslmode=require
#
# Pooler (port 6543 – use when direct fails with ENETUNREACH):
# DATABASE_URL=postgresql://postgres.REF:PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres?sslmode=require
