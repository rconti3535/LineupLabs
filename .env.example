# Copy to .env and fill in. For Railway, set these in the dashboard Variables tab.
# Supabase: use Connection string from Project Settings > Database. Add ?sslmode=require
# If password has # or @, use %23 and %40 in the URL.
DATABASE_URL=postgresql://postgres:PASSWORD@db.xxx.supabase.co:5432/postgres?sslmode=require
